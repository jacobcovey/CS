#pragma once
#include "LinkedList.h"
class Stack
{
public:
	Stack () : testing(true), size(0){}
	 ~Stack() {}

	 void push(int);
	 bool empty();
	 void pop();
	 int top();
	 int size();

protected:
	 int size;
	 bool testing;
};
